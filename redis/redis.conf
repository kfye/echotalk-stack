# 【参考留档 · 当前未挂载】
# Redis 调优已写进 docker-compose.yml 的 command，当前以 command 为准。
# 若想改用本文件，把 compose 里 redis.command 换成挂载本文件启动即可。
maxmemory 128mb
maxmemory-policy allkeys-lru
appendonly yes
appendfsync everysec
save 900 1
save 300 10
# 如需密码：解开下一行并在 app 侧配置
# requirepass change-me-redis-pass
